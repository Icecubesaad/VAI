# Dependencies
node_modules/

# Env / secrets — NEVER commit real keys. .env.example is the committed template.
.env
.env.*
!.env.example

# Expo / Metro
.expo/
.expo-shared/
dist/
web-build/

# Local native projects (managed workflow regenerates via prebuild)
android/
ios/

# Local build outputs
*.apk
*.aab
*.ipa
*.dSYM.zip
build/

# Signing / credentials — live in EAS credentials + EAS Secrets, never in git
*.jks
*.keystore
*.p8
*.p12
*.pfx
*.mobileprovision
*.provisionprofile
credentials.json
google-services.json
GoogleService-Info.plist

# Supabase local state
supabase/.branches/
supabase/.temp/

# Logs / OS / editor
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
coverage/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Browser QA static bundles (rebuilt on demand, never committed)
.expo-qa/
.expo-qa-dev/
