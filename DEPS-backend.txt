# DEPS-backend — packages required by supabase/ turf (DO NOT npm install; owner runs it)
# Format: <name> <version> — <one-line why>

npm:@supabase/supabase-js@2.45.0 — admin client + Storage in Edge Functions (pinned esm.sh import in _shared/auth.ts)
npm:inngest@3.31.0 — render-pipeline steps/retries/cron defined in supabase/inngest.md (hosted on Node, calls edge worker)
supabase-cli@2.x — `supabase db push`, `supabase functions deploy`, `supabase secrets set`
inngest-cli@1.x — local `inngest dev` against render-tryon?process=1 worker
pgvector@0.8.0 (Postgres extension "vector") — style_dna vector(64) + garments embedding vector(512) HNSW search
pgcrypto@1.3 (Postgres extension) — gen_random_uuid() + idempotency_key defaults
# No SDKs vendored for: Expo Push (raw HTTPS in _shared/push.ts), ShopStyle/LTK/Skimlinks (REST in shop-picks),
# Open-Meteo (keyless REST in _shared/plan.ts), Stripe/RevenueCat/Apple/Google webhooks (HMAC/JWS verify in secrets.md).
